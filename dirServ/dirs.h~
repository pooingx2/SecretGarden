#include "protocol.h"
#include <mysql.h>

int 
getdirectoryList(char *userId);

int 
getNode(MYSQL *con, char *userId, char *dirName, char *listBuf);

int 
creatDirectory(MYSQL *con, char *type, char *name ,char *parentNode,
			   char *metaPath,  char *root);

int 
fileUpload();

int 
fileDownload();



int
getElements(byte *dataBuf, char *token, char *tokenBuf[]);
